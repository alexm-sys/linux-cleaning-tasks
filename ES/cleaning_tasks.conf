# -----------------------------------------------------------------------------
# 🛠️ Archivo de configuración para el script de limpieza diaria
# Puedes editar estas variables para personalizar el comportamiento del script.
# Este archivo debe estar en la misma carpeta que el script o se debe ajustar la ruta.
# -----------------------------------------------------------------------------

# ¿Activar limpieza de Docker? (yes/no)
CLEAN_DOCKER="yes"

# ¿Activar rotación de logs del sistema? (yes/no)
ROTATE_LOGS="yes"

# ¿Activar limpieza del directorio /tmp? (yes/no)
CLEAN_TMP="yes"

# ¿Activar modo prueba (dry-run)? No ejecuta comandos, solo los muestra. (yes/no)
DRY_RUN="no"

# Número de días a conservar los logs del sistema
LOG_RETENTION_DAYS=7

# Archivos en /tmp no accedidos en estos días serán eliminados
TMP_RETENTION_DAYS=1

# Número máximo de días a conservar el log del propio script
MAX_LOG_AGE_DAYS=30

# Ruta del log del script (requiere permisos de escritura)
SCRIPT_LOG="/var/log/limpieza_diaria.log"
